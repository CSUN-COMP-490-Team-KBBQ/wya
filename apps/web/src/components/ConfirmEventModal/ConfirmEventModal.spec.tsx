import React from 'react';
// import { render } from '@testing-library/react';
// import ConfirmEventModal from './ConfirmEventModal';

/**
 *
 * Setup Required for component to be tested
 *
 * Pass Args:
 *
 *   event: EventData;
 *   heatMapData: HeatMapData;
 *
 */
// it('renders component', () => {
//     const { queryByText } = render(<ConfirmEventModal />);
//     expect(queryByText('ConfirmEventModalComponent')).toBeTruthy();
// });

it('runs an empty test', () => {
    expect(true).toBeTruthy();
});
